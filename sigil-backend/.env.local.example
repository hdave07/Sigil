# Copy this file to .env.local and fill in a real key.
# .env.local is gitignored (see root .gitignore's **/.env*.local pattern).
# Groq (free tier) - used by missionScopeSuggester.ts via the OpenAI-compatible endpoint.
GROQ_API_KEY=gsk_...
